# Rustchain Wallet

A tiny HD wallet for the Ergo-fork **Rustchain** network.  
Generates seed-based addresses, shows balance/UTXOs, and sends/receives via a simple CLI that talks to a node’s JSON-RPC. Supports offline signing for safer transactions.

> **Status:** WIP. Use on devnet/testnet for now.
